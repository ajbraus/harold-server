.media
  .media-left
    a(href='#')
      img.media-object(ng-src='{{campaign.img_url}}' alt='...' width="64px")
  .media-body
    h4.media-heading {{campaign.title}}
    {{campaign.location}}
    a(href="/@{{campaign.author.handle}}") {{campaign.author.name}}
